GitHub Pages for the AWGP Books OCR volunteer landing (public repo under devn03215).

Live URL (after deploy):
  https://devn03215.github.io/awgp-books/
  https://devn03215.github.io/awgp-books/?lang=hi

Same pattern as:
  https://devn03215.github.io/awgpapp-help/
  https://devn03215.github.io/awgp-share/

## How Pages is published

Pages is deployed by the **GitHub Actions** workflow `.github/workflows/deploy-pages.yml`
on every push to `main` (and via workflow_dispatch).

Source of truth for edits remains `books-landing/` on awgp-mobile → copy into the
awgp-books checkout on `main` and push. Do not rely on the legacy branch builder alone.

UI language (`?lang=hi|gu|mr`) is separate from the on-page book-language filter chips.

## Deploy updates

From awgp-mobile (source of truth):

```powershell
npx tsx scripts/deploy-books-landing.ts --push
```

That copies `books-landing/` into `c:\dev\git\awgp-books`, commits/pushes `main`, and
GitHub Actions publishes the static site.

After deploy, verify:
  https://devn03215.github.io/awgp-books/js/i18n.js
  https://devn03215.github.io/awgp-books/?lang=hi

(Hard-refresh or wait ~1 minute for CDN.)

First-time clone:

```powershell
git clone https://github.com/devn03215/awgp-books.git c:\dev\git\awgp-books
```
